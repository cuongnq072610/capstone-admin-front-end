/*
 * AddCoursePage Messages
 *
 * This contains all the text for the AddCoursePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCoursePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCoursePage container!',
  },
});
