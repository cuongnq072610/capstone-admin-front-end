/*
 * TeacherPage Messages
 *
 * This contains all the text for the TeacherPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeacherPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeacherPage container!',
  },
});
