/*
 * NoteDetailPage Messages
 *
 * This contains all the text for the NoteDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NoteDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Ask Detail Page',
  },
});
