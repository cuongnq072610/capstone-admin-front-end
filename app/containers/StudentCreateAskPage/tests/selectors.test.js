// import { fromJS } from 'immutable';
// import { selectStudentCreateAskPageDomain } from '../selectors';

describe('selectStudentCreateAskPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
