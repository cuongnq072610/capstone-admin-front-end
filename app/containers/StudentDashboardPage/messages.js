/*
 * StudentDashboardPage Messages
 *
 * This contains all the text for the StudentDashboardPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentDashboardPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentDashboardPage container!',
  },
});
