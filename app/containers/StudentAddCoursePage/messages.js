/*
 * StudentAddCoursePage Messages
 *
 * This contains all the text for the StudentAddCoursePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentAddCoursePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentAddCoursePage container!',
  },
  filter: {
    id: `${scope}.filter`,
    defaultMessage: 'Departments',
  },
});
