// import { fromJS } from 'immutable';
// import { selectNotePageDomain } from '../selectors';

describe('selectNotePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
