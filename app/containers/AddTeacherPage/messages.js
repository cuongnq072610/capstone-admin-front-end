/*
 * AddTeacherPage Messages
 *
 * This contains all the text for the AddTeacherPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddTeacherPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddTeacherPage container!',
  },
});
