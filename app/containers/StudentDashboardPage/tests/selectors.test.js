// import { fromJS } from 'immutable';
// import { selectStudentDashboardPageDomain } from '../selectors';

describe('selectStudentDashboardPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
