/*
 * ChooseRolePage Messages
 *
 * This contains all the text for the ChooseRolePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChooseRolePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChooseRolePage container!',
  },
});
