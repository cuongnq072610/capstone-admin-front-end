/*
 * StudentComposePage Messages
 *
 * This contains all the text for the StudentComposePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentComposePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentComposePage container!',
  },
});
