/*
 * NoteFolderPage Messages
 *
 * This contains all the text for the NoteFolderPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NoteFolderPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NoteFolderPage container!',
  },
});
