// import { fromJS } from 'immutable';
// import { selectStudentAddCoursePageDomain } from '../selectors';

describe('selectStudentAddCoursePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
