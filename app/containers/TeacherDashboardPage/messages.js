/*
 * TeacherDashboardPage Messages
 *
 * This contains all the text for the TeacherDashboardPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeacherDashboardPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeacherDashboardPage container!',
  },
});
