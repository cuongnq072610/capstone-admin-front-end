// import { fromJS } from 'immutable';
// import { selectHighLightFolderPageDomain } from '../selectors';

describe('selectHighLightFolderPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
