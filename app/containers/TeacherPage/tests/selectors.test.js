// import { fromJS } from 'immutable';
// import { selectTeacherPageDomain } from '../selectors';

describe('selectTeacherPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
