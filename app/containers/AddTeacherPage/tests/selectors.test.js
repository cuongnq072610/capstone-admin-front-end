// import { fromJS } from 'immutable';
// import { selectAddTeacherPageDomain } from '../selectors';

describe('selectAddTeacherPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
